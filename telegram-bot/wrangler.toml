name = "familychurch-telegram-bot"
main = "worker.js"
compatibility_date = "2024-01-01"

# Set these as secrets via: npx wrangler secret put TELEGRAM_BOT_TOKEN
# Environment variables (set via Cloudflare dashboard or wrangler secret):
# - TELEGRAM_BOT_TOKEN
# - TELEGRAM_ADMIN_ID
# - GITHUB_TOKEN
# - GITHUB_REPO
